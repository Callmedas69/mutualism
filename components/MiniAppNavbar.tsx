"use client";

/**
 * MiniAppNavbar
 *
 * Mini App Simplification: This navbar is hidden entirely.
 * Only one destination (graph) in simplified mini app, no navigation needed.
 *
 * The component is kept for future restoration if needed.
 */
export default function MiniAppNavbar() {
  return null;
}
